export interface UsersType {
  user_id: number;
  name: string;
  mail: string;
  role: string;
}

export interface UserType {
  user_id: number;
  name: string;
  mail: string;
  role: string;
  accessToken: string;
}
