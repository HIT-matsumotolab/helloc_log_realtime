export interface QuestionsType {
  question_id: number;
  name: string;
  format: string;
  user_id: number;
  mode: string;
  time_limit: number;
  number_limit: number;
}

export interface QuestionType {
  question_id: number;
  name: string;
  format: string;
  user_id: number;
  mode: string;
  time_limit: number;
  number_limit: number;
}
